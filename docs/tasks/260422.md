# 260422 작업 기록

## 1. connection 모듈 2차 전환 계속

요약: app → connection.api 의존 경계 적용

- 문제: app 서비스가 connection 내부 `AuthService`와 entity/token 메서드에 직접 의존
- 지시사항: 2차 전환 방향으로 connection 모듈 분리 계속 진행
- 작업: `PlatformConnectionFacade` / `CurrentConnection` / `TokenAccess` 도입
- 작업: auth/user controller → connection facade 호출로 전환
- 작업: repository / issue / comment service → token access result 사용으로 전환
- 작업: 설계 문서 2차 적용 상태 갱신
- 결과: connection 내부 서비스 직접 주입 제거
- 결과: app 업무 서비스의 connection entity 직접 접근 제거
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 2. repository 모듈 3차 전환

요약: 저장소 캐시 책임을 repository 물리 모듈로 이동

- 문제: repository 책임이 app 내부에 남아 issue/comment가 repository entity와 service에 직접 의존
- 지시사항: 3차 전환 진행
- 작업: `RepositoryCache`, repository JPA, `RepositoryService`, repository DTO 이동
- 작업: `RepositoryFacade` / `RepositoryAccess` 도입
- 작업: app controller / issue / comment service → repository facade 호출로 전환
- 작업: sync 상태 기능을 `shared-kernel`로 이동
- 결과: app의 repository 내부 service/entity 직접 의존 제거
- 결과: repository 모듈 컴파일 경계 생성
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 3. issue 모듈 4차 전환

요약: 이슈 캐시 책임을 issue 물리 모듈로 이동

- 문제: issue 책임이 app 내부에 남아 comment가 issue entity와 service에 직접 의존
- 지시사항: 4단계 전환 진행
- 작업: `IssueCache`, issue JPA, `IssueService`, issue DTO 이동
- 작업: `IssueFacade` / `IssueAccess` 도입
- 작업: app issue controller / comment service → issue facade 호출로 전환
- 결과: app의 issue 내부 service/entity 직접 의존 제거
- 결과: issue 모듈 컴파일 경계 생성
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 4. comment 모듈 5차 전환

요약: 댓글 캐시 책임을 comment 물리 모듈로 이동

- 문제: comment 책임이 app 내부에 남아 업무 모듈 분리가 미완료
- 지시사항: 5차 전환 진행
- 작업: `CommentCache`, comment JPA, `CommentService`, comment DTO 이동
- 작업: `CommentFacade` 도입
- 작업: app comment controller → comment facade 호출로 전환
- 결과: app의 comment 내부 service/entity 직접 의존 제거
- 결과: comment 모듈 컴파일 경계 생성
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 5. 모듈 경계 6차 검증

요약: 의존 방향과 Spring/JPA scan 회귀 테스트 추가

- 문제: 업무 모듈 물리 분리 후 의존 방향과 scan 누락을 자동으로 막는 테스트 부족
- 지시사항: 6차 변경 진행
- 작업: Gradle 모듈 의존 방향 테스트 추가
- 작업: app → 업무 모듈 내부 service/domain/repository 직접 import 차단 테스트 추가
- 작업: facade bean 등록과 JPA entity scan 검증 추가
- 결과: 모듈 경계 회귀 방지 기준 확보
- 결과: Spring context / JPA metamodel 검증 강화
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 6. 내부 패키지 7차 정리

요약: public API와 internal 구현 패키지 경계 명확화

- 문제: 업무 모듈 분리 후에도 `domain`, `repository`, `service` 패키지명이 공통으로 남아 소유권이 흐림
- 지시사항: 7차 전환 진행
- 작업: connection / repository / issue / comment 내부 구현 → `*.internal.*` 이동
- 작업: facade → 자기 모듈 internal service 참조로 갱신
- 작업: JPA entity scan 테스트와 모듈 경계 테스트 갱신
- 결과: public API와 internal 구현 네이밍 분리
- 결과: 타 모듈 internal import 차단 규칙 강화
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 7. API DTO 8차 정리

요약: 모듈 DTO를 public API 패키지로 이동

- 문제: request/response DTO가 공통 `dto.*` 패키지에 남아 모듈 공개 API 소유권이 흐림
- 지시사항: 8차 전환 진행
- 작업: auth / repository / issue / comment / sync DTO → 각 모듈 `api.dto` 이동
- 작업: controller / facade / service import 갱신
- 작업: 공통 모듈 DTO 패키지 재사용 차단 테스트 추가
- 결과: DTO와 facade가 같은 public API 경계에 위치
- 결과: 외부 REST JSON 계약 유지
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 8. Gradle 멀티 모듈 9~11차 마무리

요약: 의존성 노출 축소 + Spring/JPA 경계 명시 + 리뷰 문서 생성

- 문제: 일부 모듈 의존이 `api`로 전이 노출
- 문제: JPA entity/repository scan이 루트 scan에 암묵 의존
- 지시사항: 9차~11차를 모두 진행하고 리뷰 문서 생성
- 작업: issue/comment/shared-kernel 의존 scope 축소
- 작업: 모듈별 Spring/JPA configuration 추가
- 작업: Gradle `api` scope 검증 테스트 추가
- 작업: 설계 문서와 리뷰 문서 갱신
- 결과: 모듈 공개 계약 노출 최소화
- 결과: persistence scan 경계 명시
- 결과: 후속 코드 리뷰 기준 문서 확보
- 검증: `.\gradlew.bat clean :app:bootJar test` 성공

## 9. Gradle 멀티 모듈 2~11차 리뷰 문서 정리

요약: 2차 이후 전환 흐름을 리뷰용 단일 문서로 정리

- 문제: 9~11차 리뷰 문서만 있어 전체 전환 흐름 확인이 어려움
- 지시사항: 2차부터 11차까지 리뷰 문서 작성
- 작업: 단계별 변경 / 확인 포인트 / 리스크 정리
- 작업: 전체 전환 흐름 그래프와 최종 의존성 그래프 추가
- 결과: 후속 코드 리뷰 기준 통합
- 결과: 단계별 검토 관점 명확화
