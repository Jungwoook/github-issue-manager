# 260507 작업 기록

## 1. Rate Limit 재처리 유즈케이스 정리

요약: 1차 수동 재처리와 이후 자동 재처리 확장 흐름 분리

- 문제: rate limit 설계에 구체 유즈케이스 부족
- 지시사항: 1차 수동 재처리 / 이후 자동 재처리 기준 반영
- 작업: GitHub rate limit 상태 수집 유즈케이스 추가
- 작업: rate limit 동기화 중단 / 실패 이력 조회 / 수동 재처리 흐름 추가
- 작업: 저장소 / 이슈 단위 수동 재동기화 흐름 추가
- 작업: 자동 재처리는 TODO 유즈케이스로 분리
- 결과: `14-platform-rate-limit-recovery-plan.md` 유즈케이스 보강
- 결과: `09-core-use-cases.md` 운영 유즈케이스 목록 추가

## 2. Rate Limit 유즈케이스 상세 추가

요약: core use case 문서에 UC-20~UC-26 상세 흐름 추가

- 문제: 운영 유즈케이스 목록만 있고 상세 흐름 부족
- 지시사항: 유즈케이스 상세도 추가
- 작업: rate limit 상태 수집 / 동기화 중단 기록 상세 작성
- 작업: 실패 이력 조회 / 수동 재처리 상세 작성
- 작업: 저장소 / 이슈 수동 재동기화 상세 작성
- 작업: 자동 재처리 예약은 TODO 상세로 작성
- 결과: `09-core-use-cases.md` 상세 유즈케이스 보강

## 3. Rate Limit 모듈 영향 정리

요약: rate limit 추가에 따른 신규/변경 모듈 경계 정리

- 문제: rate limit 구현 시 application / platform 책임 증가
- 지시사항: 모듈 추가 또는 변경 사항 정리
- 작업: application sync run / failure / rate limit / recovery 영역 정리
- 작업: 기존 facade 변경 지점과 SyncState 유지 기준 정리
- 작업: platform GitHub rate limit 추출 / 실패 모델 변환 책임 정리
- 작업: app 복구 API 추가 범위 정리
- 작업: connection / repository / issue / comment / shared-kernel 유지 기준 정리
- 결과: `14-platform-rate-limit-recovery-plan.md` 모듈 영향 섹션 추가

## 4. Rate Limit 시퀀스 다이어그램 추가

요약: UC-20~UC-26 흐름을 sequence diagram으로 보강

- 문제: 유즈케이스 상세는 있으나 모듈 간 호출 순서가 부족
- 지시사항: 유즈케이스 시퀀스 다이어그램 추가
- 작업: rate limit 수집 / 동기화 중단 기록 다이어그램 추가
- 작업: 실패 이력 조회 / 수동 재처리 다이어그램 추가
- 작업: 저장소 / 이슈 수동 재동기화 다이어그램 추가
- 작업: 자동 재처리 예약 TODO 다이어그램 추가
- 결과: `11-use-case-sequence-diagrams.md` 운영 복구 흐름 보강

## 5. 시퀀스 다이어그램 라벨 추상화

요약: method/API 중심 화살표를 동작 중심 표현으로 변경

- 문제: 시퀀스 다이어그램이 구현 method 이름 중심으로 읽힘
- 지시사항: 큰 틀의 동작 방식 중심으로 변경
- 작업: register / refresh / retry 등 method 라벨을 행동 이름으로 변경
- 작업: DTO / entity 응답명 라벨을 업무 결과 표현으로 변경
- 작업: rate limit / 재처리 다이어그램의 저장/마감 라벨을 운영 행동으로 정리
- 결과: `11-use-case-sequence-diagrams.md` 다이어그램 가독성 개선

## 6. 실패 응답 사용자 안내 기준 추가

요약: 실패 직후 재처리 정보를 응답으로 전달하는 기준 명확화

- 문제: 수동 재처리 구조에서 사용자 안내 방식 불명확
- 지시사항: 실패 시 재처리 관련 안내 과정을 문서화
- 작업: 실패 응답에 syncRunId / failureId / retryable / nextRetryAt 포함 기준 추가
- 작업: 별도 푸시/메일 알림은 1차 제외로 명시
- 작업: 실패 이력 조회는 실패 직후 확인 또는 사후 복구 진입점으로 정리
- 결과: `14-platform-rate-limit-recovery-plan.md`, `09-core-use-cases.md` 안내 흐름 보강

## 7. Rate Limit 설계 불일치 정리

요약: SyncState/SyncRun, use case 진입점, rate limit 소유 기준 확정

- 문제: SyncState와 SyncRun의 상태 조회 기준 중복
- 문제: 복구 통합 facade가 application 비대화 방지 기준과 충돌
- 문제: 기존 facade 명칭과 신규 use case-per-class 기준 혼재
- 문제: rate limit snapshot 소유 모듈 미확정
- 작업: UC-09 / UC-16은 SyncState 응답 기준으로 확정
- 작업: SyncRun은 복구/운영 상세 이력 기준으로 분리
- 작업: SyncRecoveryApplicationFacade 제거, 개별 use case 진입점으로 정리
- 작업: 기존 facade는 전환 전 흐름으로만 언급하고 신규 복구 흐름에는 추가 금지
- 작업: platform_rate_limit_snapshots 소유를 application으로 확정
- 결과: 09 / 11 / 14 문서 간 설계 기준 정합성 보정

## 8. SyncFailure 의존과 상태 매핑 보정

요약: sync.failure 역방향 의존 제거와 SyncRun → SyncState 매핑 확정

- 문제: SyncFailureService가 재동기화 use case에 의존하는 것으로 서술됨
- 문제: PARTIAL_SUCCESS / RATE_LIMITED의 SyncState 반영 기준 미정
- 작업: sync.failure 의존에서 재동기화 use case 제거
- 작업: sync.failure는 실패 레코드 저장/조회/상태 변경만 담당하도록 정리
- 작업: recovery use case가 SyncFailureService를 사용하는 방향으로 확정
- 작업: SyncRun 상태별 SyncState 반영 표 추가
- 결과: PARTIAL_SUCCESS / RATE_LIMITED는 SyncState에 FAILED로 요약하고 메시지에 상세 사유 기록

## 9. 구현 단계 압축

요약: 5단계 구현 계획을 3단계 + TODO로 축소

- 문제: 구현 단계가 세분화되어 1차 구현 범위가 길게 보임
- 지시사항: 구현 단계를 압축
- 작업: 이력 기반 / rate limit 감지 / 수동 복구 API 3단계로 재구성
- 작업: 실패 응답 사용자 안내를 2단계에 포함
- 작업: 수동 재처리와 수동 재동기화를 3단계로 통합
- 작업: 자동 재처리 / Webhook / GitLab 확장은 후속 TODO로 분리
- 결과: `14-platform-rate-limit-recovery-plan.md` 구현 단계 단순화
