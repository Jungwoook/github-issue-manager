# 260502 작업 기록

## 1. 아키텍처 구조 개선 계획 문서화

요약: 복잡한 모듈 의존을 application 중심 구조로 단순화하는 개선안 정리

- 문제: 현재 구조 / 모듈 간 의존 과다
- 문제: platform / connection / 업무 모듈 책임 혼재
- 지시사항: 구조 개선 방향 문서 작성
- 작업: 목표 의존 방향 / 모듈 책임 / 전환 단계 정리
- 작업: application 계층 도입 기준 작성
- 작업: application 별도 Gradle 서브모듈 결정 반영
- 작업: application 내부 패키지 / 유스케이스 분리 규칙 추가
- 작업: 목표 구조 다이어그램과 모듈 책임 설명 불일치 보정
- 작업: issue/comment의 다른 업무 모듈 직접 호출 금지 기준 반영
- 작업: shared-kernel 축소 기준을 PlatformType only로 확정
- 작업: SyncState 클러스터 application 이동 주의사항 반영
- 작업: not found 예외를 모듈별 예외로 분리하는 기준 반영
- 작업: 단계별 전환 계획을 3단계로 압축
- 작업: 목표 구조 다이어그램의 shared-kernel 라벨을 PlatformType only로 보정
- 작업: 14번 개선안 비교 후 13번 문서 보강
- 작업: application SyncState JPA 소유 예외 / sync 패키지 / 대표 흐름 sync 기록 반영
- 작업: 업무 모듈 간 직접 참조 금지와 모듈별 not found 예외 책임 명시
- 결과: `13-architecture-improvement-plan.md` 추가
- 결과: 코드 개선 전 목표 구조 기준 확보

## 2. application 경계 생성

요약: app controller → application facade 경계 추가

- 문제: app이 업무 모듈 facade를 직접 호출
- 지시사항: 13번 문서 1단계 전환 진행
- 작업: application Gradle 서브모듈 추가
- 작업: auth / repository / issue / comment application facade 추가
- 작업: app 의존을 application 중심으로 변경
- 검증: `.\gradlew.bat test`
- 결과: HTTP 조립과 use case 조립 경계 분리

## 3. use case orchestration 이전

요약: token 조회 / remote 호출 / sync 기록을 application으로 이동

- 문제: 업무 모듈이 platform remote 호출과 sync 기록을 직접 조립
- 지시사항: 13번 문서 2단계 전환 진행
- 작업: PlatformRemoteFacade 제거
- 작업: repository / issue / comment의 platform remote 호출 의존 제거
- 작업: SyncState 클러스터를 application으로 이동
- 작업: platform -> connection 의존 제거
- 검증: `.\gradlew.bat test`
- 결과: application이 remote 호출, cache 반영, sync 기록 조립
